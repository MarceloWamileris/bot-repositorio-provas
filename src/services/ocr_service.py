class OCRService:

    @classmethod
    async def extrair_texto(
        cls,
        caminho,
    ):

        print(
            f"OCR: {caminho}"
        )

        texto = ""

        print(
            f"Texto extraído: {texto}"
        )

        return texto